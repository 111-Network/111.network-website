{
  "name": "111-network-web",
  "version": "0.1.0",
  "private": true,
  "description": "111 Network Web - Monorepo for web applications and packages",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
